spring:
  threads:
    virtual:
      enabled: true
  data:
    mongodb:
      host: localhost
      port: 27017
      database: product-db
      username: root
      password: password
      authentication-database: admin
app:
  maintenance:
    message: "Sistema Operativo - Version 1.0"

management:
  endpoints:
    web:
      exposure:
        include: "refresh, health"

server:
  port: 8080
  error:
    include-message: always
    include-stacktrace: never